
//js

document.addEventListener('DOMContentLoaded', function() {
    let tiles = document.querySelectorAll('.tile');

    let turn = "X";

    //    Iterate over each tile and allow moves in empty tiles and 
    //        create turns to switch either X or O
    
    //Create reset button and a  win statement when a wining combination is met
});
